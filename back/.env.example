# Session Secret
# Generate a strong secret with: openssl rand -hex 32
# IMPORTANT: Never commit the actual .env file with your real secret!
SESSION_SECRET=your_session_secret_here_replace_with_random_value

# Database Configuration (optional - defaults in config.js)
# DATABASE_URL=mongodb://localhost:27017
# DB_NAME=fe

# Server Ports (optional - defaults in config.js)
# PORT=8088
# SSL_PORT=8089
